#include<stdio.h>
int main()
{
    int side,square,cube;
    printf("enter the value of side :");
    scanf("%d",&side);
    square = side*side;
    cube = side*side*side;
    printf("square is : %d\n",square);
      printf("cube is : %d",cube);
    return 0;


}