#include<stdio.h>
int main()
{
       float cprice , sprice;
       printf("enter the price :");
       scanf("%d", &cprice, &sprice);

       if(cprice<sprice)
       {
        profit=(sprice-cprice)
         printf("%d\n",profit);
       }
       else
       {
        loss=( cprice-sprice)
        printf("%d\n",loss); 

       }
       return 0;
       
}