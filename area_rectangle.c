#include<stdio.h>
int main ()
{
    int length,breath,area;
    printf("enter the value of length");
    scanf("%d",&length);
    printf("enter the value of breath");
    scanf("%d",&breath);
    area=length*breath;
    printf("%d",area);
    return 0;
}